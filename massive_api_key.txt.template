MASSIVE_API_KEY=

# Instructions:
# 1. Paste your Massive (formerly Polygon.io) API key after the "=" above (no quotes, no spaces).
# 2. Save a copy of this file as "massive_api_key.txt" (drop the ".template" suffix) in the same folder.
# 3. massive_api_key.txt is gitignored, so the real key never gets committed.
#
# Note: this is the SAME key used elsewhere for Massive lookups (news/catalyst, short-interest,
# market/options data) — rolling_watchlist (3).py reads it from the MASSIVE_API_KEY environment
# variable. If the new data pull is a genuinely different Massive account/tier (not just a new
# endpoint on the existing account), rename this file/variable before use so it doesn't collide
# with the existing key.
