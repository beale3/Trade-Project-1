# Decisions log — HELM (`trade`)

Append-only. IDs `D-TRADE-NNN`, allocated MAX+1 by the Lead at rebase (renumber-on-collision, keep both).
Columns: **ID · date · decision · why · decided-by · → PROPAGATION**. A decision is **not closed** until
every downstream doc it binds is named in its row and updated **in the same commit** (protocol 3 / LL-25),
or the row states `→ PROPAGATION: none` + why. Rulings state **scope: instance | class** (protocol 18).
`🔒-pending` = the Lead's recommended default, awaiting the Director's explicit confirmation (LL-38); it is
scaffolded against but **not ruled** until confirmed.

| ID | date | decision | why | by | → PROPAGATION |
|---|---|---|---|---|---|
| D-TRADE-001 | 2026-08-01 | **Team founded** on Foundation Kit v2.2.0; greenfield. *(class)* | start the governed team | Lead | AGENT-COORDINATION, PROJECT-CONFIG, README, this repo skeleton — done this commit |
| D-TRADE-002 | 2026-08-01 | **Wave template = GREENFIELD** (W0→W3+, §7). *(class)* | no app code exists (validated) | Lead | app-design/stage-plan.md — done this commit |
| D-TRADE-003 | 2026-08-01 | **Stack = Node/TS · Fastify · Postgres/Supabase · React/Vite** (recommended default). *(class)* | kit default fits a SaaS surface | Lead 🔒-pending | gate-spec.md commands, AGENT-COORDINATION §3 lanes, stage-plan — done this commit. **OPEN framework lock:** a Python data/ML lane may be added if the product proves quant-heavy → reopen before W1. |
| D-TRADE-004 | 2026-08-01 | **Cost model = BILLED PER-USE** → FinOps governs real dollars; **B4 metered chokepoint arms from the spine**; a $/day self-tally auto-kill. *(class)* | market-data + LLM calls are metered | Lead 🔒-pending LOCK | PROJECT-CONFIG §2/§4, gate-spec (money-truth leg), oracle-boundary (FinOps/BE-Data rows), stage-plan W1 — done this commit; **needs explicit Director yes** |
| D-TRADE-005 | 2026-08-01 | **Roster = 14 seats** (core spine + AI/ML · AIQ · FinOps · Legal · Data-Eng). *(class)* | AI/finance SaaS family | Lead 🔒-pending LOCK | PROJECT-CONFIG §3, AGENT-COORDINATION §2/board, oracle-boundary rows, role-bootstrap-scripts — done this commit; **needs explicit Director yes** |
| D-TRADE-006 | 2026-08-01 | **Compliance bright-lines armed as gates:** (a) **no secret in repo/logs** (SEC/market-data keys) — a committed key pattern FAILS; (b) **money-truth** — a billed call bypassing the metered chokepoint FAILS; (c) **provider-ToS taint** — a provider SDK/host outside its sanctioned module FAILS. *(class)* | SEC/financial + billed-spend surface | Lead + SecOps(to confirm) | gate-spec.md legs, oracle-boundary (SecOps/FinOps/Legal rows) — done this commit; legs **arm at their build wave** (SKIP-visible until then) |
| D-TRADE-007 | 2026-08-01 | **Isolation:** kit methodology crosses; product content/brand/design never cross (LL-4). No project-specific rule stated at founding. *(class)* | isolation bars content, not process | Lead | AGENT-COORDINATION isolation banner, PROJECT-CONFIG §6 — done this commit; Director may add a specific rule → reopen |
| D-TRADE-008 | 2026-08-01 | **Money-truth surface declared:** the single metered chokepoint (Lane 2, BE-Data) is the only path for billed provider calls; every call writes an append-only spend-ledger row + passes the fail-closed governor. *(class)* | high-invariant surface, spec-complete-before-build | Lead | B2 pillar ④/⑥, B4 L4, gate-spec money-truth leg, oracle-boundary BE-Data/FinOps — done this commit |
| D-TRADE-009 | 2026-08-01 | **B9 Validation Gauntlet — recommended RUN** before any design/build (new opportunity). *(class)* | gate the opportunity before building it (LL-20) | Lead 🟡 DIRECTOR-PENDING | stage-plan pre-build order, PROJECT-CONFIG §4, foundation/README open-decisions — done this commit; **awaits Director run/skip + the product paragraph** |
| D-TRADE-018 | 2026-08-01 | 🟠 **PROVIDER-TAINT FINDING (SEV2, pending GA) — SecOps ToS-taint complete + FinOps dollar-cost corroboration.** (a) **Polygon/Massive = HIGH taint** — default ToS bars commercial/redistribution/"investment-strategy derivative works" (incompatible with strawman `<1.1>` on 4 counts); Business tier + real-time exchange fees needed, or go EDGAR-only near-term. **FinOps confirms in dollars:** self-serve $29–199 tiers are exactly the incompatible Non-Professional tier; a commercial Business/Professional tier is **quote-only**, and real-time additionally drags in **quote-only OPRA/Nasdaq-UTP/NYSE exchange fees** not shown on the public pricing page — real-time-vs-delayed is a real, currently-unpriced lever on `<2.1>`. (b) **In-hand "SEC API key" issuer UNCONFIRMED** — not public keyless EDGAR; likely a reseller whose ToS re-opens the taint (also flips EDGAR from FinOps's assumed $0.00-marginal to a real per-key-cost provider if so). (c) Supabase MEDIUM — service_role/DB-pwd B5, data-class lines → Legal. **Needs a Director provider/tier decision + a Legal `<4.3>` ruling (regulated advice / licensable strategy?) BEFORE `<2.1>`/`<1.1>` lock or build.** *(class — provider policy)* | SecOps `docs/security/tos-taint-review.md` (source+read-date+revision, LL-58/62); FinOps `docs/finops/cost-model.md` corroborates independently | SecOps + FinOps → Lead; **escalated to Director + Legal** | canonical `<2.1>` + `<4.3>` (absorbed), open-items-ledger §A — done this commit; final severity → GA |
| D-TRADE-023 | 2026-08-01 | 🔒 **The equity-side tool (`tools/rolling_watchlist.py`) gets a browser UI, not a desktop app** — the full dashboard (stat strip, watchlist table, Guardrail #1 detail panel, S3 score breakdown, P&D phase stepper, intraday pivot chart, the trade simulator's results), reusing the Director's already-approved "Rolling Watchlist" mockup design, wired to the real Massive-backed backend instead of illustrative data. **Scoped to this equity side-tool only — does NOT reopen HELM Phase-1's own `<1.1>`/`<3.5>` "no web/API surface" framing**, which stays as-is for the options-screener validation work. Director explicitly directed team assignment over solo build. *(instance — this tool, not a HELM-wide reversal)* | Director: "build the simulator as a browser interface and not a desktop app" + "send the assignments to the team" | **Director** | working-log.md (assignments below), Designer re-activated, Architect/DevOps/AI-ML assigned — done this commit |
| D-TRADE-022 | 2026-08-01 | 🔒 **Ratify ADR-0001's stack, module layout, lane re-cut, and the directional-correctness label form (OP-1/2/3).** Python core (Node/Fastify/React dropped) · single `helm/` package, disjoint by directory · 5-lane re-cut (A ingest+universe+store · B screener · C validation-engine · D validation-audit, independent · E infra/spend) · label = two-tier (continuous forward-return regression + a volatility-scaled OHLCV-only directional binary), horizons ~25/35/45 DTE. Lead-ratified (not escalated) — three-way technical convergence (Architect + AI/ML + AIQ already agree), well-grounded in the ADR's citations, and implements the Director's own `<1.1>`/`<1.4>` ruling rather than introducing new product scope. *(class — binds Phase-1 build)* | ADR-0001 (`docs/adr/ADR-0001-phase1-validation-tool.md`), co-authored/converged by Architect+AI/ML, reconciled against D-TRADE-021 on rebase | **Lead** (ratified; Director co-sign still required at the wave-entry GO per ADR §12) | canonical `<3.5>`/`<3.6>` (this commit) · oracle-boundary.md (NN-1..9 legs to add) · charter §3 lane cut (to update) · stage-plan P1 preconditions (P-4 narrowed) |
| D-TRADE-021 | 2026-08-01 | 🔒 **Ratify the Phase-1 pre-registered clearance bar (AIQ's proposal, `docs/eval/methodology-draft.md`):** a screener component is **CLEARED** only if it beats a naive baseline out-of-sample under **BOTH** LOO-CV **AND** 5-fold CV (≥30 seeds), with **≥90% of seeds agreeing**; **NOT CLEARED** otherwise; **VOID** regardless of the above on any leakage/lookahead/contamination finding. Ratified by the Lead (methodology, not a business/roster/cost lock) — this is not novel, it exactly matches the short-interest study's own successful precedent (96.7%/93.3% seed-agreement, the one study of 4 that cleared robustly) and is the same discipline that caught the catalyst study's fragile false-positive (LOO said "beats," 5-fold said "0/3," 50-seed sweep showed a coin flip). *(class — binds every Phase-1 component test, not just the first)* | AIQ pre-registered before any result exists (avoids post-hoc bar-shopping); Lead ratifies against established precedent, no new Director lock needed | **Lead** (ratified, not self-ruled by AIQ — protocol 17/14 builder≠judge preserved: AI/ML doesn't set its own bar either) | `docs/eval/methodology-draft.md` (AIQ's, already reflects this) · canonical-design.md `<3.4>` (addendum, this commit) · AI/ML notified so both seats work against the same ratified number |
| D-TRADE-020 | 2026-08-01 | 🔒 **`<1.1>` LOCKED — HELM = a personal options-signal validation tool** (not commercial SaaS). Phase 1: validate the existing rules-based options screener (trend/momentum/breakout/volume components) against a liquid-optionable-stock universe, via directional-correctness walk-forward CV (same discipline as the 4 equity studies). Phase 2 (deferred): full option-P&L simulation + a from-scratch predictive occurrence model. Supersedes the founding SaaS strawman entirely — re-authored, not patched (LL-19). *(class — the product)* | 3-round Director elicitation (personal-vs-commercial → phase scope → universe → success metric → final lock, all explicit AskUserQuestion confirmations) | **Director** | canonical-design.md §1-5 (full re-author), PROJECT-CONFIG.md, AGENT-COORDINATION.md roster+board, stage-plan.md, oracle-boundary.md (AI/ML+AIQ re-scope, tenant-isolation N/A) — done this commit; **all live seats notified by message this session** |
| D-TRADE-019 | 2026-08-01 | **Money-truth chokepoint cost shape (D-TRADE-008 refinement):** of the 4 adopted providers, only **LLM tokens are true per-use variable COGS**; Polygon/Massive (flat subscription) and direct SEC EDGAR (free) are $0-marginal standing floor; Supabase is flat-plan + slow usage overage. The chokepoint's `$/day` auto-kill is, in practice, an LLM-token meter — but ALL provider calls still route through it and write a ledger row (rate/ToS governance + reconciliation), even at $0.00. Per-signal COGS is unmeasured by construction (no engine yet); caps arm tight, rise on measured evidence. *(class — cost architecture)* | FinOps `docs/finops/{cost-model,governor-spec}.md` — DRAFT, self-checked (gate ② GROUND); not yet GA-reconciled (gate ③); no dollar values ruled (HUMAN/Director, D-TRADE-004 still 🔒-pending) | FinOps → Lead | canonical `<3.2>` note pending Lead absorption; chokepoint invariant checklist (FinOps's portion drafted, open co-sign slots: SDE1/BE-Data · QA · SecOps) — locks at W1 |
| D-TRADE-006 | *(update)* | (a) no-secret + (c) provider-taint "SecOps (to confirm)" → **CONFIRMED** via `docs/security/{tos-taint-review,key-denylist,b5-secret-approval-checklist}.md` (each leg carries a planted negative control, LL-48). Propagation CLOSED. | — | SecOps + Lead | legs arm at W0 (K/T static), SKIP-visible until then; DevOps wires · GA audits · QA re-runs |
| D-TRADE-017 | 2026-08-01 | 🔴 **ENVIRONMENT BLOCKER — dev toolchain ABSENT on host.** Verified by Lead + DevOps in BOTH Git Bash and PowerShell: `node`·`npm`·`npx`·`pnpm`·`docker`·`gh` all NOT FOUND (only `git`). **W0 build cannot produce a green gate until the Director installs Node LTS + pnpm + Docker Desktop (+ gh CLII)** and puts them on the PATH the agent shells use. Also gates the DB-baseline capture (the MCP `npx` server can't launch without Node). Ports validated free (3000/5173/5432/54321). *(class — blocks all build)* | root cause under both DevOps blockers; the "validated environment" must state the truth (LL-1) | **Director** (install) | AGENT-COORDINATION §1 Toolchain row, open-items-ledger §A, docs/roles/devops/harness-design.md §F, docs/infra/supabase.md ⚠️Reconcile — done/aligned this commit |
| D-TRADE-016 | 2026-08-01 | **Stand up 3 seats: Principal Architect · SDE1 · FinOps.** Architect = **Fable 5 · Max (LOCKED — the sole frontier seat)**; SDE1 + FinOps = Opus 4.8 · High. **SDE1 is not a kit archetype → mapped to the Backend (Data & Domain) IC, Lane 2** (`packages/domain`, `packages/db`, the money-truth chokepoint) — reads `roles/be-data` PROFILE; Director may reassign to Lane 1 or make additional. **Phase:** FinOps has light pre-build work (cost-model the adopted providers — Supabase/Polygon/SEC/LLM tokens — real dollars per D-TRADE-004); Architect + SDE1 HOLD (no product `<1.1>` + no-build D-TRADE-010 = nothing to architect/build). *(instance — these seats)* | Director standing up architecture + first builder + cost governor | **Director** | AGENT-COORDINATION board (Architect/FinOps → spawning, Backend-Data row = SDE1), docs/roles/sde1/PROFILE.md (= be-data), role-bootstrap-scripts — done this commit; each claims its own row on spawn |
| D-TRADE-015 | 2026-08-01 | **Stand up 4 seats: DevOps · SecOps · AI/ML · AIQ** (each Opus 4.8 · High, own clone `Trade - <Role>`). Roster → 19 seats live/seated. **Phase-appropriate:** SecOps has an ACTIVE pre-build task (provider ToS-taint on SEC EDGAR · Polygon · Supabase + key denylist); DevOps does infra-prep (capture DB baseline via the read-only MCP; design gate harness) but HOLDS on W0 scaffold (D-TRADE-010); AI/ML + AIQ HOLD (no product `<1.1>` = no engine yet). *(instance — these seats)* | Director standing up the pre-build/infra + AI cluster | **Director** | AGENT-COORDINATION live board rows (4), role-bootstrap-scripts.md blocks — done this commit; each seat claims its own board row on spawn |
| D-TRADE-014 | 2026-08-01 | **Wire the official Supabase MCP connector, project-scoped `.mcp.json`, READ-ONLY**, scoped to `--project-ref=zyscsnhiymitpfdhjuci`, token via `${SUPABASE_ACCESS_TOKEN}` (env, never committed). Write access stays closed by default (money-truth surface) — opening it is a later Director-gated change. *(class — team-wide connector)* | Director asked to complete setup incl. the MCP connector; team-wide DB introspection | **Director** + Lead | `.mcp.json` (new), docs/infra/supabase.md (MCP section + Director enable steps), `.env.example` (token placeholder) — done this commit. **Director enables interactively** (create PAT → set env var → `/mcp` approve); this non-interactive session cannot run the OAuth/enable flow. **CONNECTED ✅ 2026-08-01 (Director-confirmed)** — live in clone-scoped sessions |
| D-TRADE-013 | 2026-08-01 | **Adopt Supabase project `zyscsnhiymitpfdhjuci` as the DB/backend** (`https://zyscsnhiymitpfdhjuci.supabase.co`), concretizing the Postgres/Supabase stack (D-TRADE-003). Reachability verified. Secrets (service_role key, DB password) are **B5 — Director/SecOps install into a gitignored `.env`/secret store; never in chat or repo**; the agent never handles them. *(instance — this project)* | Director directed connecting to this Supabase project | **Director** | docs/infra/supabase.md (new), `.env.example` (placeholders), PROJECT-CONFIG §5 (DB + secrets rows), gate-spec leg K target + leg T (Supabase SDK/host), canonical `<3.x>` DB note — done this commit. **SecOps ToS-taint + B5 approval pending seat spawn** |
| D-TRADE-012 | 2026-08-01 | **Canonical remote = `https://github.com/beale3/Trade-Project-1`** (Director-supplied), superseding the recommended default `ShupeCapital/trade`. Origin already exists with a placeholder `main` (README, `c79ceb5`). Slug/infra identity (`trade`, `D-TRADE-`, clone scheme) unchanged. *(instance — the remote; org identity ShupeCapital untouched)* | Director gave the actual remote; Designer flagged the README/remote mismatch (protocol 16) | **Director** + Lead | all 22 `ShupeCapital/trade` refs across 8 docs → `beale3/Trade-Project-1` (README, CLAUDE.md, charter, PROJECT-CONFIG, foundation/README, role-bootstrap-scripts, ledger, activity-log) — done. **Baseline PUSHED ✅ 2026-08-01** — origin/main @ `3d3f1aa`, foundation + kit verified on origin, no secret leaked; Designer released to sync |
| D-TRADE-011 | 2026-08-01 | **Seat the Design Lead ("Designer")** now, in its own clone `Trade - Designer` (Opus 4.8 · High). Roster grows to 15 seats; taste-tier (mostly HUMAN, Director is approver of record). *(instance — this seat; roster lock D-TRADE-005 still pending overall)* | Director wants the Designer stood up in its own context window | **Director** | AGENT-COORDINATION §2 roster + live board, oracle-boundary.md (Design row), role-bootstrap-scripts.md (block 15), docs/roles/design/PROFILE.md — done this commit |
| D-TRADE-010 | 2026-08-01 | **⏸ NO CODE BUILD / NO WAVE DISPATCH IS AUTHORIZED.** Current phase = **foundation only**. Every wave in `stage-plan.md` (incl. W0) is the documented PLAN, **not an available action**. Building unblocks only after the pre-build gate clears: product defined (`<1.1>`) → B9 viability/blueprint (if run) → Director build-GO. No build role needs spawning until there is build work for it. *(class)* | Director ruling — we are not building any code yet | **Director** | stage-plan.md (banner + W0 line), foundation/README.md (§3 + next-steps), open-items-ledger.md (§B, §D) — done this commit |
| D-TRADE-024 | 2026-08-01 | 🔒 **D-TRADE-023's ticker-input open item resolved: build a real input control** (ticker box + Scan button in the browser dashboard), not the lighter hardcoded-watchlist-config-file fallback. Satisfies protocol 10's Director-approval leg for this specific UI addition — proportionate to the existing approved design system, not a fresh mockup round-trip. *(instance — this dashboard's ticker-input mechanism only, does not reopen ADR-0002's other decisions)* | Designer + AI/ML flagged this as new UI surface not in the original approved mockup or ADR-0002 scope, correctly declined to invent it; Director chose between the two options the Lead presented | **Director** | AGENT-COORDINATION §LIVE BOARD (Designer next-up), Designer dispatched by message this commit — done this commit |
| D-TRADE-031 | 2026-08-04 | 🔒 **Add a `min_float` guardrail parameter to `tools/rolling_watchlist.py`'s `scan_guardrail_criteria()`, mirroring the existing `max_float`, + a float-distribution-by-outcome-bucket analysis script.** Grounded in a real reference implementation (`day_trade_toolkit.py`/`premarket_scan.py`, Director-supplied ZIP, inspected directly) that already wires this pattern up. **Neither bound is backtested** — same caveat the reference implementation states plainly, and the exact conclusion HELM's own completed float study already reached with real numbers (float via Massive `/stocks/vX/float`: current-only, 77.6% coverage; via SEC-API.io: 83.2%/36.5% unusable — NO-GO on both, `float_gates` stays `False`). Adding the parameter is low-risk (mirrors existing code, off by default absent real float data) but should not be presented as more functional than it is. *(instance — this dashboard/scanner feature only, does not reopen the float study's NO-GO verdict)* | Director asked to incorporate a real, working reference implementation into the scanner; low-risk, well-precedented parameter addition | **Director**, dispatched to AI/ML (write-lane owner of `tools/rolling_watchlist.py`) | `tools/rolling_watchlist.py` (AI/ML's write-lane, dispatched by message, not yet built) |
| D-TRADE-030 | 2026-08-04 | 🔒 **Ratify + absorb ADR-0001 Revision 2 into canonical `<3.5>`/`<3.6>`.** The two-leg validation contract (Leg A entry-signal, Leg B trailing-stop exit-rule vs. fixed-holding baseline), the trailing-stop formula (§6.3), NN-10 (parameter isolation), the builder≠judge extension to the feature layer, the anti-cherry-pick rule, and the 4-state verdict schema (incl. UNMEASURED/D-TRADE-029) are now canonical. *(class — binds Phase-1's technical validation design)* | Full protocol-17 CRITICAL-tier review completed: Architect authored, AIQ's audit found 3 real objections (import-boundary gap, Leg-B baseline-N leakage, grid cherry-pick) — all fixed in the ADR text and independently re-verified by AIQ against the actual revised text (not a summary) before co-signing (`d75b252`). AI/ML co-signed Leg A + trailing-stop mechanics + raised 2 precision fixes, both folded in. Lead verified every citation at source across the whole review chain, not just attested reports. | **Lead** (ratified — both required co-signs are in, this is absorbing a fully-reconciled technical design, the same path D-TRADE-022 took for R1) | canonical `<3.5>`/`<3.6>` (this commit) |
| D-TRADE-029 | 2026-08-04 | 🔒 **Minimum-trigger-count floor for ADR-0001 R2/R3's Leg-A pattern components: 30 events.** Below 30 firings of a binary pattern trigger (bull-flag, ABCD, etc.) across the backtest window, that component's verdict is **UNMEASURED** — not NOT CLEARED — same "no data behind it" treatment the float study already established as precedent (distinct absence-classes, not a judgment call). The number is not invented fresh — anchored to the ≥30-seed statistical-power assumption already baked into the ratified D-TRADE-021 clearance bar. *(class — binds every thin-firing Leg-A component, not just the first one tested)* | AIQ (independent oversight seat) proposed the concrete number during their ADR-0001 R2 audit; AI/ML (builder) proposed the floor's existence and agreed the number; neither self-ruled it — Lead ratifies against the same precedent-matching logic D-TRADE-021 used (not novel, methodology not business/cost/roster) | **Lead** (ratified, not escalated — matches D-TRADE-021's own path) | ADR-0001 R3 (Architect's write-lane, to state this explicitly alongside the other 3 fixes) · canonical `<3.6>` — **PROPAGATION: deferred until ADR-0001 R3 lands as a whole** (this ratifies one parameter inside a still-open ADR revision; absorbing canonical piecemeal ahead of the full revision would risk the doc stating something not yet structurally complete) |
| D-TRADE-036 | 2026-08-30 | 🔒 **P-4 pre-registration LOCKED (OP-1/2/3), per Director's explicit authorization to use the Lead's own recommended defaults, converting ADR-0001 §10's "e.g." ranges into fixed numbers.** **OP-1 (trailing-stop grid):** `trail_pct ∈ {5, 8, 12}`, `init_stop_pct ∈ {2, 3}` (6 cells); **primary, clearance-eligible cell = trail_pct=8, init_stop_pct=3** (the middle trail setting + the looser init floor, to reduce early-noise whipsaw while still bounding max loss) — the other 5 cells are sensitivity-only, never a clearance claim (anti-cherry-pick, AIQ objection #3). **OP-2 (Leg-A evaluation horizon):** the studies' existing 1d/1w/1m set — already concrete in the ADR, no change needed, just confirming as locked. **OP-3 (Leg-B baseline `N`):** a **fully pre-registered fixed N = 5 trading days** (not the train-fold-derived-median alternative the ADR also offered) — chosen as "the simplest leakage-free option" the ADR itself flags as preferred, and horizon-matched to the 1w Leg-A window; `N=1` and `N=21` (~1mo) recorded as fixed-N sensitivity checks only. *(class — binds the actual first CV run; explicitly does not modify D-TRADE-021's bar or NN-10 itself, only fills in NN-10's parameters)* | Director authorized the Lead to select concrete values from the ADR's already-documented recommendation ranges rather than continue waiting on a fresh Director/AI/ML/AIQ round; **flagged plainly: this bypasses ADR-0001 §12's own stated expectation that AIQ co-signs "the label design + NN-10 + the baseline" before wave-entry GO** — the Director chose to short-circuit that for these three numbers specifically, this is not an independent AIQ validation standing in for one | **Director** (authorized the shortcut) + **Lead** (selected the values) — **NOT AIQ-cosigned**, unlike D-TRADE-029/030's numbers | canonical `<3.6>` (P-4 marker resolved, this commit) · open-items-ledger.md (item 9 closed, this commit) · ADR-0001 §10 — **named, not yet done** (Architect's write-lane; the ADR's own "e.g." phrasing should be updated to match these locked values) |
| D-TRADE-035 | 2026-08-30 | 🔒 **P-3 RESOLVED — `helm/universe` DROPS for Phase 1**, per the Architect's recommendation (ADR-0001 §9 P-3), Director-ratified. The Phase-1 backtest cohort is the studies' existing event-defined datasets + user-supplied `--tickers` (the scanner's status quo) — no maintained universe list. NN-5/§4-§5's Data-Eng references drop with the lane (the seat was never spawned; no residual duty needed since there's no lane left to cover). *(class — removes a planned module from Phase-1 scope entirely, not a deferral)* | Director ratified the Architect's own recommendation directly, same-day as lifting P-1 | **Director** | canonical `<2.2>` (marker resolved, this commit) · open-items-ledger.md (item 8 closed, this commit) · ADR-0001 §4/§5/§9 — **named, not yet done** (Architect's write-lane; the "CONDITIONAL — likely DROPS" module-table language needs converting to a plain drop) |
| D-TRADE-034 | 2026-08-30 | 🔴 **P-1 (D-TRADE-010) LIFTED — scoped strictly to HELM Phase-1 build.** Director build-GO: AI/ML/AIQ/SDE1/FinOps/DevOps may now build against ADR-0001 R2's module ownership map (§4) — `helm/screener`, `helm/validation/{engine,audit}`, `helm/storage`, `helm/spend`, the trailing-stop mode in `tools/rolling_watchlist.py`, and DevOps's gate-harness scaffold (`docs/roles/devops/harness-design.md` → real code). **Explicitly does NOT cover Phase 2** — D-TRADE-033 (`breakout_model`/"Predictive Model 7.0") stays HELD; D-TRADE-010 was never phase-scoped, so a Phase-1 lift doesn't imply a Phase-2 one, and canonical `<1.4>`'s Phase-2 boundary is still an open item. *(class — the single biggest unblock in this project's history; scope is explicit, not implied)* | Director, direct authorization: "Lift P-1 for Phase 1 work," same message that ratified P-3/P-4 and asked the Lead to check P-5 | **Director** | open-items-ledger.md (item 1 closed, this commit) · AGENT-COORDINATION.md LIVE BOARD banner + seat Next-up rows (this commit) · stage-plan.md's W0 gate — **named, not yet done** (needs its own "P-1 cleared" banner update, not yet touched this commit) |
| D-TRADE-033 | 2026-08-30 | 🔒 **The `breakout_model` / "Predictive Model 7.0" pipeline (`Trade/{config,data_pipeline,dataset_builder,features,labeling,main,train_model,catalyst_features,test_synthetic}.py`, currently untracked/uncommitted in the separate `Trade/` repo) is LOGGED and HELD — this is the "from-scratch predictive breakout-occurrence model" canonical `<1.4>` already names as Phase 2, out of scope. D-TRADE-010's build freeze is not phase-scoped and covers it regardless of phase.** Lead independently verified (not just attested): all 9 files exist on disk, `test_synthetic.py` runs with **exit code 0, all 8 tests pass** (labeling correctness, no-lookahead checks on features/catalyst/vwap/consolidation/parabolic-curvature, a 6-fold walk-forward smoke run). **This is unit/synthetic-level verification only — explicitly NOT the D-TRADE-021 OOS/CV clearance bar** (beats naive baseline OOS under LOO+5-fold, ≥90% seed agreement, independently re-derived by AIQ); not presented as more validated than that. No further build/training authorized under this entry — held pending **both** P-1 (D-TRADE-010, general) and a dedicated Phase-2 scope decision (Phase-1 hasn't itself cleared P-1 yet). *(class — bars this pipeline from any further build/dispatch until explicitly authorized, same shape as D-TRADE-032)* | Director surfaced the pipeline directly (unlike Guardrail v2.1, reported before landing invisibly) and asked for formal reconciliation rather than either continuing silently or unwinding real tested work | **Director** (surfaced + requested logging) + **Lead** (independent verification, ratified per D-TRADE-032 precedent) | open-items-ledger.md (new held item, this commit) · activity-log.md (recorded, this commit) — canonical-design.md **not** touched (`<1.4>` already correctly describes this as Phase 2/out of scope, nothing to change) · `Trade/` repo itself untouched — separate, ungoverned repo, no commits made there |
| D-TRADE-032 | 2026-08-14 | 🔒 **`docs/guardrail-v2.1/` (§3-§9: SI-Gate adjustments, Rel-Vol Tail, Tradability Floor, corrected S3, Composite scoring) ruled EXPLORATORY, NOT CANONICAL.** Not entering HELM Leg A/B; does **not** modify ADR-0001 OP-4's already-validated/not-relitigated component list. Not eligible for dispatch to any seat without an explicit Director D-TRADE assignment **and** a protocol-17 AIQ validation. **Block B's EDGAR-mirror plumbing fix is explicitly distinguished and separately authorized** — allowed because it corrected an ingestion-field-mapping bug in code that wraps existing production infra (`Trade/edgar_client.py`), not because it's an exception to D-TRADE-010; delivered and verified (`382c514`). *(class — bars all Guardrail v2.1 §3-§9 content from HELM/any dispatch until explicitly authorized)* | The commit landed via an ungrouped session (no working-log append, no D-TRADE number, not Lead-dispatched) and revises components (SI-Gate/S3/Composite) that ADR-0001 OP-4 currently treats as settled — Lead flagged the ambiguity rather than assume either way; Director ruled directly | **Director** | `docs/guardrail-v2.1/README.md` (status banner, this commit) · open-items-ledger.md (recorded, this commit) · activity-log.md (recorded, this commit) — canonical-design.md **not** touched, nothing to absorb since this explicitly doesn't enter HELM |
| D-TRADE-028 | 2026-08-04 | 🔴 **HELM Phase-1 drops options entirely — SECOND MAJOR REVISION of `<1.1>`, same weight as D-TRADE-020.** HELM validates the equity scanner already in this repo (`tools/rolling_watchlist.py`) producing plain stock buy signals exited via a **trailing-stop rule** (new, not yet built — `simulate_day_trades()` currently has only a fixed stop/target). No calls/puts, no delta targeting, no DTE window, no options-chain/IV data, no 0DTE-backtest-engine reuse. Universe reopens (no more liquid-optionable large/mid-cap requirement). **P-2 (locate the missing options-screener/0DTE-engine ZIPs) is MOOT, not resolved-by-search** — the artifact was never actually missing; it's `tools/rolling_watchlist.py`, confirmed already fully in-repo (verified against the Director's own pasted source — every function present, repo copy is a strict superset). D-TRADE-021's clearance bar and NN-1's no-lookahead invariant are explicitly NOT reopened by this pivot — validation discipline carries forward unchanged. *(class — the product, same scope tier as D-TRADE-020)* | Director, direct instruction: *"Ignore all Option language... implement standard trading logic using basic buy/sell signals and trailing-stop rules... Do not apply any Option-related logic,"* explicitly confirmed via AskUserQuestion to apply to HELM Phase-1 itself, not only the D-TRADE-023 dashboard side-tool | **Director** | canonical-design.md §1–3/§5 (full re-author per LL-19/protocol 19, this commit) · open-items-ledger P-2 row (this commit) · ADR-0001 revision — **named, not yet done** (Architect's write-lane, dispatched by message) · stage-plan.md/oracle-boundary.md — **named, not yet done** (flagged for the seats that read them, not yet re-authored) |
| D-TRADE-027 | 2026-08-02 | 🔒 **SEC-API.io tier confirmed: Personal & Startups** (narrows D-TRADE-026 — the issuer was never in doubt for this, only which paid tier). Per FinOps's already-measured pricing (`cost-model.md §2.3`, re-fetched from `sec-api.io/pricing`): **$49/mo billed annually or $55/mo monthly, 50GB included, $0.30/GB overage** — not the Business tier ($199/$239, 100GB). This is the actual, currently-active line, not an estimate. *(instance — this account's tier only)* | Director confirmed directly | **Director** | `docs/finops/cost-model.md`/`governor-spec.md` cap table — **named, not yet updated** (FinOps write-lane, flagged by message) · open-items-ledger §A row 3 — narrowed item now fully closed, this commit |
| D-TRADE-026 | 2026-08-01 | 🔒 **`<2.1>` resolved — the in-hand `..\Trade\sec_api_key.txt` IS a real, live SEC-API.io key**, not an unidentified/reseller credential. Verified by the Lead with a direct authenticated call to `https://api.sec-api.io` using the value (never the whole `SEC_API_KEY=<value>` line — the first two attempts 403'd on that exact mistake before the format was corrected): **HTTP 200, real EDGAR data returned** (10,000+ matched filings, a live 10-K/A sample). Corroborated by the Director's own logged-in sec-api.io account (screenshot, browser session). The key value itself was never printed, logged, or committed — only its length and `NAME=VALUE` structure were inspected to diagnose the initial 403. **Material consequence:** this is a **paid personal-tier subscription** ($49–239/mo per FinOps's D-TRADE-019 pricing research), not free/keyless public EDGAR — the cost model's EDGAR-is-$0 assumption no longer holds for this credential and needs reframing. *(instance — this specific key/account; does not change the EDGAR-as-provider taint finding itself, D-TRADE-018)* | Live API verification (HTTP 200 + real filing data) + Director's own account screenshot, cross-checked by the Lead, not self-attested by either alone | **Lead** (verified at source, not escalated — this is a factual confirmation of an already-flagged open item, not a new policy call) | canonical `<2.1>` (this commit) · open-items-ledger §A row 3 (this commit) · `docs/finops/cost-model.md` + `docs/security/tos-taint-review.md` — **named, not yet updated** (FinOps/SecOps write-lanes; flagged to both by message) |
| D-TRADE-025 | 2026-08-01 | 🔒 **D-TRADE-023's simulator-UI open item resolved: build the full trade-simulator results panel** (trade-by-trade log, P&L curve, win-rate/final-P&L/trade-count stats, halt reason) — not a summary-only version, not deferred. The `simulatedTrades` object is already returned by `/api/scan` (ADR-0002 §3); this is frontend-only, no backend change. *(instance — this dashboard's simulator panel only)* | D-TRADE-023's own decision text already scoped "the trade simulator's results" as in-dashboard; the approved mockup just never had a panel for it — Designer correctly flagged rather than inventing it | **Director** | AGENT-COORDINATION §LIVE BOARD (Designer next-up), Designer dispatched by message this commit — done this commit |

**Open framework/arch locks that must resolve before the core wave:** the stack Python-lane question
(D-TRADE-003), and everything downstream of the **product paragraph** (`canonical-design <1.1>`), which
is `NOT DECIDED` and blocks a real W1 feature design.
